name: Deployment pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches: [main]
    types: [opened, synchronize]

env:
  # Check if we should deploy (main branch push without #skip in commit message)
  OK_TO_DEPLOY: ${{ github.ref == 'refs/heads/main' && github.event_name == 'push' && !contains(join(github.event.commits.*.message, ' '), '#skip') }}
  # Run Playwright tests if commit contains #play or it's a pull request
  RUN_PLAYWRIGHT: ${{ contains(join(github.event.commits.*.message, ' '), '#play') || github.event_name == 'pull_request' }}

jobs:
  deployment_pipeline:
    runs-on: ubuntu-latest
    # Skip entire CI pipeline if commit message contains #noci
    if: ${{ !contains(join(github.event.commits.*.message, ' '), '#noci') }}
    
    services:
      postgres:
        image: postgres:18
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: dummypassword1234
          POSTGRES_DB: test_db
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      
      - name: Install root dependencies
        run: npm ci
      
      - name: Install frontend dependencies
        run: npm --prefix frontend ci
      
      - name: Install backend dependencies
        run: npm --prefix backend ci
      
      - name: Check style
        run: npm run lint
      
      - name: Run backend tests
        run: npm run test:backend
        env:
          SECRET: test-secret-key-for-ci
          TEST_DATABASE_URL: postgres://postgres:dummypassword1234@localhost:5432/test_db
      
      - name: Run frontend tests
        run: npm run test:frontend

      - name: Install Playwright Browsers
        if: env.RUN_PLAYWRIGHT == 'true'
        run: npx playwright install --with-deps
      
      - name: Run Playwright tests
        if: env.RUN_PLAYWRIGHT == 'true'
        env:
          SECRET: test-secret-key-for-ci
          TEST_DATABASE_URL: postgres://postgres:dummypassword1234@localhost:5432/test_db
        run: npx playwright test
      
      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() && env.RUN_PLAYWRIGHT == 'true' }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
      
      # - name: Build application
      #   run: npm run build