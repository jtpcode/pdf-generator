{
  "name": "pdf-generator-root",
  "private": true,
  "scripts": {
    "install_dependencies": "npm --prefix frontend install && npm --prefix backend install",
    "lint": "npm --prefix frontend run lint && npm --prefix backend run lint"
  }
}